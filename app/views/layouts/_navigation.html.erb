<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to icon(:bicycle, 'Home'), root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Results', results_path %></li>
        <li><%= link_to 'Races', races_path %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right" id="user-login">
        <% if signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= icon(:user, "Logged in as #{current_user.first_name}") %><span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'Logout', sign_out_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <%= link_to 'Login', sign_in_path, class: 'btn btn-sm btn-success navbar-btn' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
